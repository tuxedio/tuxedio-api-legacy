- provide(:title, "Explore")

.row
  .col-md-4.col-md-offset-4
    .page-header
      %h1.center
        Explore
        .small
          %span.glyphicon.glyphicon-glass
          See what's happening around town.

- if customer_signed_in? and @trip_count > 0
  = render 'explore_with_cart'

-else
  = render 'explore_without_cart'


